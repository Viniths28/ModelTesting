from setuptools import setup, find_packages

setup(
    name="flow_engine",
    version="0.1.0",
    description="Graph-based Flow Builder Engine",
    packages=find_packages(),
    python_requires=">=3.8",
) 